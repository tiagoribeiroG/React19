import { useFormState } from 'react-dom';

const EstadoDoFormulario = () => {
    const acaoDoFormulario = (formData) => {
// min40
    }
  return 
  <form action={acaoDoFormulario}></form>
}

export default EstadoDoFormulario
